<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".view.UserPanel.OrderConfirm"
    android:orientation="vertical"
    android:gravity="center"
    android:background="#EDEDED">

    <include
        android:id="@+id/include_order_confirm_toolbar"
        layout="@layout/custom_toolbar"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Your Order Successfully"
        android:textSize="30sp"
        android:textStyle="italic"
        android:textColor="@color/colorPrimary"
        android:layout_gravity="center"/>

    <Button
        android:id="@+id/confirm_go_to_home"
        android:layout_width="200sp"
        android:layout_height="wrap_content"
        android:text="go to home"
        android:background="@drawable/cart_button_corner_white"
        android:layout_gravity="center"
        android:layout_marginTop="100sp"/>

</LinearLayout>